using system_obslugi_serwisu.Database;

namespace system_obslugi_serwisu.Types;

public class DBContext
{
    /*public static Result<User> GetUser()
    {
        var userResult = User.Create("testmail@supermail.org", "superUser123");

        if (userResult.IsFailure)
        {
            return Result<User>.Failure(userResult.Error);
        }

        return Result<User>.Success(userResult.Value);
    }*/
}